#ifndef HORARIO_HPP
#define HORARIO_HPP

/**
 * @brief CLASSE PLACEHOLDER (Incompleta). Representa a entidade Horario.
 *
 * * Esta definição de classe vazia serve unicamente para resolver dependências
 * de compilação na classe Professor e ProfessorService, que utilizam um
 * std::vector<Horario>.
 * * O código de domínio (atributos e métodos) deve ser adicionado
 * posteriormente.
 */
class Horario {};

#endif