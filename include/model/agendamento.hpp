#ifndef AGENDAMENTO_HPP
#define AGENDAMENTO_HPP

/**
 * @brief CLASSE PLACEHOLDER (Incompleta). Representa a entidade Agendamento.
 *
 * * Esta definição de classe vazia serve unicamente para resolver dependências
 * de compilação na classe Aluno e AlunoService, que utilizam um
 * std::vector<Agendamento>.
 * * O código de domínio (atributos e métodos) deve ser adicionado
 * posteriormente.
 */
class Agendamento {};

#endif